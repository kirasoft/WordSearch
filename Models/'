import random
import string

class Grid:
    
    grid_points = []

    def __init__(self, dimension):
        self.dimension = dimension 
        self.grid_points = []


    def create_grid_points(dimension):
       min = 0
       max = dimensions 

       for x in range(min, max):
           for y in range(min, max):
               rand_alpha = random.randint(
               grid_points.append(x, y)

        for x in range(1, dimension + 1):
            grid_points.append(Grid(x, y))


